# Runtime-only Dockerfile that uses the already-built JAR in target/
# This avoids needing a Maven builder image during `docker build`.
FROM eclipse-temurin:17-jre-jammy
WORKDIR /app
# Copy the already-built jar from the workspace
COPY target/ProxyFHIR-0.0.1-SNAPSHOT.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
